timer.jquery is a simple timer plugin using jquery. It shows a text based timer inside a div or span or any other valid HTML element.
It has 4 methods => start, pause, resume and get_seconds.

You can specify the number of seconds to initialize the timer from and get the currently displayed time in seconds.